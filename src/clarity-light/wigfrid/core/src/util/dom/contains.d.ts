/**
 * Checks whether an HTML element contains another.
 *
 * @param parent Parent element.
 * @param child Child element.
 * @return True if the parent element contains the child element.
 */
export declare function contains(parent: any, child: any): boolean;
export declare function containsFocus(e: HTMLElement): boolean;
